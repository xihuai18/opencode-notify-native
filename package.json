{
  "name": "opencode-notify-native-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Repository tooling for @leo000001/opencode-notify-native direct notification plugin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xihuai18/opencode-notify-native.git"
  },
  "scripts": {
    "build": "npm run build --prefix opencode-plugin",
    "test": "npm test --prefix opencode-plugin",
    "typecheck": "npm run typecheck --prefix opencode-plugin",
    "build:all": "npm run build && npm run test",
    "pack:plugin": "npm --prefix opencode-plugin pack"
  }
}
